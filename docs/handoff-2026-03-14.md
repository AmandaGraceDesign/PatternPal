# Handoff — March 14, 2026

## Branch: `seam-analyzer`

### Completed Today

1. **Ruler unit conversion fix** — Fixed `formatLabel()` in `Ruler.tsx`: px mode now divides by `pixelsPerUnit` (was showing raw screen pixels), cm mode removed double `* 2.54` conversion. Fixed `getTickSpacing()` for px mode to space ticks at clean pattern-pixel intervals scaled by `pixelsPerUnit`.

2. **Viewport rendering spec & plan** — Designed and planned a full rewrite of the canvas rendering to fix pixelation on zoom. The current system pre-scales tiles to a small intermediate canvas + `toDataURL` roundtrip before tiling, which destroys quality. The new system draws directly from the full-resolution source image via 9-arg `drawImage`, rendering only the visible viewport.

### Ready to Execute: Viewport-Based Rendering

**Spec:** `docs/superpowers/specs/2026-03-14-viewport-rendering-design.md`
**Plan:** `docs/superpowers/plans/2026-03-14-viewport-rendering.md`

The plan has 5 tasks across 4 chunks:

1. **PatternTiler rewrite** (`src/lib/tiling/PatternTiler.ts`) — New API accepts source image, scale factor, pan offset, repeat type. Calculates visible tile grid, clips to viewport, draws via 9-arg `drawImage`. No intermediate canvas.

2. **Zoom/pan state** (`app/page.tsx`, `PatternControlsTopBar.tsx`) — Zoom range 10-800% (was 0-200 with `* 0.2` indirection). Add `panX`/`panY` state. Fit-to-viewport calculation on image upload.

3. **Main canvas rendering** (`PatternPreviewCanvas.tsx`) — Remove `displayZoomToActualZoom()`, remove intermediate canvas + `toDataURL`, replace `overflow-auto` scroll with pointer-event panning (RAF-throttled). Canvas matches container exactly. Tile outlines account for all repeat types. Ruler formulas updated for direct zoom.

4. **SeamInspector** (`SeamInspector.tsx`) — Remove `createTileGrid()` and `createPattern()`. Direct viewport rendering from source. Zoom extended to 800%. `imageSmoothingEnabled = false` at ALL zoom levels.

5. **Integration testing** — Ruler edge cases, Scale Preview compatibility, debug log cleanup.

**Key decisions:**
- Tasks 1-3 commit together (API change spans files)
- Task 4 (SeamInspector) commits independently
- Scale factor = `(zoom / 100) * (96 / dpi)` — at 100% zoom + 300 DPI, scale = 0.32
- Placeholder rendering unchanged
- DPR handled by caller via `ctx.scale(dpr, dpr)` before PatternTiler

### Files Modified This Session
- `src/components/canvas/Ruler.tsx` — unit conversion + tick spacing fix (committed)
- `docs/superpowers/specs/2026-03-14-viewport-rendering-design.md` — spec (committed)
- `docs/superpowers/plans/2026-03-14-viewport-rendering.md` — implementation plan (committed)

### To Start Next Session
Resume on `seam-analyzer` branch. Open the plan at `docs/superpowers/plans/2026-03-14-viewport-rendering.md` and begin execution from Task 1, Step 1.
