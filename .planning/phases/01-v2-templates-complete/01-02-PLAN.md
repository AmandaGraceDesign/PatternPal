---
phase: 01-v2-templates-complete
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts
autonomous: true
requirements:
  - MOCK-01
  - MOCK-03

must_haves:
  truths:
    - "All 6 V1 mockups (onesie, fabric-swatch, wallpaper, throw-pillow, wrapping-paper, journal) render through V2 engine as single-zone photo-based templates"
    - "Every V2 template (migrated + existing procedural) has a human-readable sizeLabel in format: W×H\" (Wcm×Hcm) Product Name"
    - "V2 gallery tab contains all 7+ templates (6 migrated + kids dress + existing procedural), each renderable without errors"
    - "Wrapping paper photo and procedural variants have disambiguating display names"
  artifacts:
    - path: "src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts"
      provides: "6 migrated V1 templates + sizeLabels on all templates"
      contains: "'onesie'"
    - path: "src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts"
      provides: "Wrapping paper display name disambiguation"
      contains: "Wrapping Paper ("
  key_links:
    - from: "src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts"
      to: "public/mockups/"
      via: "productBase.imagePath and productBase.maskPath references to V1 photo/mask files"
      pattern: "imagePath.*mockups/"
    - from: "src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts"
      to: "src/lib/mockups/mockupEngineV2/templates/types.ts"
      via: "All templates conform to MockupV2Template type"
      pattern: "MockupV2Template"
---

<objective>
Migrate all 6 V1 mockups into the V2 template registry as single-zone photo-based templates, and add sizeLabels (with inches + cm) to every V2 template including existing procedural ones.

Purpose: MOCK-01 requires all V1 mockups to render through V2. MOCK-03 requires every template to have a human-readable sizeLabel. This plan completes both by authoring 6 new template entries and backfilling sizeLabels on all existing V2 templates.

Output: 6 new photo-based V2 templates in templateRegistry.ts, sizeLabels on every template, wrapping paper variants disambiguated.
</objective>

<execution_context>
@/Users/amandacorcoran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amandacorcoran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v2-templates-complete/01-CONTEXT.md
@.planning/phases/01-v2-templates-complete/01-RESEARCH.md
@.planning/phases/01-v2-templates-complete/01-01-SUMMARY.md

@src/lib/mockups/mockupEngineV2/templates/types.ts
@src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts
@src/lib/mockups/mockupTemplates.ts
@src/__tests__/templateRegistry.test.ts

<interfaces>
<!-- Key types and contracts from Plan 01 output. -->

From src/lib/mockups/mockupEngineV2/templates/types.ts (after Plan 01):
```typescript
export interface MockupZone {
  id: string;
  maskPath: string;
  patternArea: { x: number; y: number; width: number; height: number };
  perspective: { topSqueeze: number; bottomSqueeze: number };
  displacement: { intensity: number; wrinkleFreq: number; type: DisplacementType };
  blend: { mode: BlendMode; opacity: number };
  physicalWidth?: number;  // ← added by Plan 01
}

export type MockupV2Category =
  | 'apparel' | 'home-goods' | 'stationery' | 'accessories'
  | 'gifting' | 'wallpaper' | 'fabric';

export interface MockupV2Template {
  id: string;
  name: string;
  description: string;
  category: MockupV2Category;
  canvasSize: { width: number; height: number };
  patternArea: { x: number; y: number; width: number; height: number };
  perspective: { topSqueeze: number; bottomSqueeze: number };
  displacement: { intensity: number; wrinkleFreq: number; type: DisplacementType };
  blend: { mode: BlendMode; opacity: number };
  zones?: MockupZone[];
  lighting: { enabled: boolean; intensity: number };
  physicalSize: { width: number; height: number; unit: 'in' | 'cm' };
  sizeLabel?: string;
  productBase:
    | { type: 'procedural'; brightness: number; shape: DisplacementType }
    | { type: 'image'; imagePath: string; maskPath?: string };
}
```

V1 Asset Inventory (from RESEARCH.md):
| V1 ID | Photo | Mask | Canvas | patternArea | Physical (in) | Category |
|-------|-------|------|--------|-------------|---------------|----------|
| onesie | /mockups/onesie.png | /mockups/onesie_mask_new.png | 1024x1024 | 0,0,1024,1024 | 10.5x18 | apparel |
| fabric-swatch | /mockups/fabric_swatch.png | /mockups/fabric_swatch_mask.png | 1024x1024 | 0,0,1024,1024 | 12x12 | fabric |
| wallpaper | /mockups/wallpaper.png | /mockups/wallpaper_mask.png | 1024x1024 | 0,0,1024,1024 | 86x60 | wallpaper |
| throw-pillow | /mockups/throw_pillow.png | /mockups/throw_pillow_mask.png | 1024x1024 | 0,0,1024,1024 | 18x18 | home-goods |
| wrapping-paper | /mockups/wrapping_paper.png | /mockups/wrapping_paper_mask.png | 1024x1024 | 0,0,1024,1024 | 8x8 | gifting |
| journal | /mockups/journal.png | /mockups/journal_mask.png | 1024x1024 | 0,0,1024,1024 | 5.5x8.5 | stationery |

sizeLabel Format (locked decision):
`{W}×{H}" ({Wcm}×{Hcm}cm) {Product Name}`
Cm conversion: inches × 2.54, rounded to 1 decimal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 6 V1 migration templates with two-pass lighting approach</name>
  <files>src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts</files>
  <action>
Add 6 new template entries to `mockupV2Templates` in `templateRegistry.ts`. This follows the locked two-pass migration decision: first match V1 output exactly (lighting off), then enhance with V2 lighting overlay.

**Pass 1 — V1-faithful templates (lighting OFF):**

Create all 6 templates with these invariants:

- `perspective: { topSqueeze: 0, bottomSqueeze: 0 }` — photo has natural perspective
- `displacement: { intensity: 0, wrinkleFreq: 0, type: 'flat-surface' }` — no procedural displacement
- `lighting: { enabled: false, intensity: 0 }` — lighting OFF for V1 fidelity pass
- `productBase: { type: 'image', imagePath: '...', maskPath: '...' }` — photo-based
- `canvasSize: { width: 1024, height: 1024 }` — all V1 masks are 1024x1024
- `patternArea: { x: 0, y: 0, width: 1024, height: 1024 }` — all V1 templates use full canvas

**Template IDs and data** (keep original V1 IDs per locked decision):

1. `'onesie'`: name="Baby Onesie", category='apparel', physicalSize={width:10.5, height:18, unit:'in'}, imagePath='/mockups/onesie.png', maskPath='/mockups/onesie_mask_new.png', sizeLabel='10.5×18" (26.7×45.7cm) Baby Onesie', blend={mode:'multiply', opacity:0.9}

2. `'fabric-swatch'`: name="Fabric Swatch", category='fabric', physicalSize={width:12, height:12, unit:'in'}, imagePath='/mockups/fabric_swatch.png', maskPath='/mockups/fabric_swatch_mask.png', sizeLabel='12×12" (30.5×30.5cm) Fabric Swatch', blend={mode:'multiply', opacity:0.9}

3. `'wallpaper'`: name="Wallpaper", category='wallpaper', physicalSize={width:86, height:60, unit:'in'}, imagePath='/mockups/wallpaper.png', maskPath='/mockups/wallpaper_mask.png', sizeLabel='86×60" (218.4×152.4cm) Wallpaper', blend={mode:'multiply', opacity:0.9}

4. `'throw-pillow'`: name="Throw Pillow", category='home-goods', physicalSize={width:18, height:18, unit:'in'}, imagePath='/mockups/throw_pillow.png', maskPath='/mockups/throw_pillow_mask.png', sizeLabel='18×18" (45.7×45.7cm) Throw Pillow', blend={mode:'multiply', opacity:0.9}

5. `'wrapping-paper'`: name="Wrapping Paper (Gift Box)", category='gifting', physicalSize={width:8, height:8, unit:'in'}, imagePath='/mockups/wrapping_paper.png', maskPath='/mockups/wrapping_paper_mask.png', sizeLabel='8×8" (20.3×20.3cm) Wrapping Paper (Gift Box)', blend={mode:'multiply', opacity:0.9}

6. `'journal'`: name="A5 Journal", category='stationery', physicalSize={width:5.5, height:8.5, unit:'in'}, imagePath='/mockups/journal.png', maskPath='/mockups/journal_mask.png', sizeLabel='5.5×8.5" (14×21.6cm) A5 Journal', blend={mode:'multiply', opacity:0.9}

Also update the existing `'wrapping-paper-v2'` template:
- Change `name` from `'Wrapping Paper'` to `'Wrapping Paper (Flat Sheet)'` to disambiguate from the photo version (per locked decision about duplicate product display name suffixes).

Place the new templates in category-grouped sections:
- onesie under `// --- Apparel ---`
- fabric-swatch under a new `// --- Fabric ---` comment
- wallpaper under `// --- Wallpaper ---`
- throw-pillow under `// --- Home Goods ---`
- wrapping-paper under `// --- Gifting ---`
- journal under a new `// --- Stationery ---` comment

Each template needs a `description` field — keep them short and descriptive (e.g., "See your pattern on a baby onesie").

After adding all 6 templates, run `npm run dev` and visually spot-check at least 2 migrated templates (e.g., onesie and throw-pillow) in the V2 gallery tab to confirm they render and visually match V1 output (pattern tiles correctly within mask, no lighting artifacts, photo base visible). Log the result in a comment at the top of the commit message.

**Pass 2 — Enable V2 lighting overlay:**

Once V1 fidelity is confirmed, update all 6 migrated templates to enable lighting:
- Change `lighting: { enabled: false, intensity: 0 }` to `lighting: { enabled: true, intensity: 0.25 }`

Run `npm run dev` again and confirm the lighting overlay adds subtle depth without washing out the pattern or breaking the photo base.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/templateRegistry.test.ts --reporter=verbose 2>&1 | grep -E "(MOCK-01|PASS|FAIL|onesie|fabric-swatch|wallpaper|throw-pillow|wrapping-paper|journal)"</automated>
  </verify>
  <done>All 6 V1 template IDs exist in V2 registry with productBase.type=image, correct categories, correct physicalSize values, correct image/mask paths. V1 visual fidelity confirmed with lighting OFF before lighting was enabled. Final state has lighting enabled (intensity 0.25). wrapping-paper-v2 renamed to disambiguate. MOCK-01 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add sizeLabels to all existing V2 procedural templates</name>
  <files>src/lib/mockups/mockupEngineV2/templates/templateRegistry.ts</files>
  <action>
Add `sizeLabel` to every existing V2 procedural template that does not yet have one. Use the locked format: `{W}×{H}" ({Wcm}×{Hcm}cm) {Product Name}`.

Centimeter conversion: `inches × 2.54`, rounded to 1 decimal place.

Templates needing sizeLabel (compute from their physicalSize values):

| ID | physicalSize (in) | sizeLabel |
|----|------------------|-----------|
| womens-skirt | 20×24 | `20×24" (50.8×61cm) Women's Skirt` |
| tablecloth | 60×40 | `60×40" (152.4×101.6cm) Tablecloth` |
| curtain | 42×84 | `42×84" (106.7×213.4cm) Curtain` |
| blanket | 50×60 | `50×60" (127×152.4cm) Blanket` |
| nursery-wall | 120×96 | `120×96" (304.8×243.8cm) Nursery Wall` |
| gift-bag | 10×13 | `10×13" (25.4×33cm) Gift Bag` |
| wrapping-paper-v2 | 30×20 | `30×20" (76.2×50.8cm) Wrapping Paper (Flat Sheet)` |
| wallpaper-roll | 20×33 | `20×33" (50.8×83.8cm) Wallpaper Roll` |
| silk-scarf | 36×36 | `36×36" (91.4×91.4cm) Silk Scarf` |
| phone-case | 3×6 | `3×6" (7.6×15.2cm) Phone Case` |
| desk-mat | 35×16 | `35×16" (88.9×40.6cm) Desk Mat` |

Note: The 6 migrated templates and kids dress already have sizeLabels from Task 1 and Plan 01 respectively. This task fills in the remaining procedural templates to satisfy MOCK-03 ("every template has a sizeLabel").

After adding all sizeLabels, run the full test suite. All MOCK-01, MOCK-02, and MOCK-03 tests should pass.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/templateRegistry.test.ts --reporter=verbose 2>&1 | grep -E "(MOCK-03|sizeLabel|PASS|FAIL|every)" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Every V2 template (migrated + existing procedural + kids dress) has a sizeLabel in the locked format "W×H\" (Wcm×Hcm) Product Name". All MOCK-03 tests pass. TypeScript compiles clean. Full vitest suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green (all MOCK-01, MOCK-02, MOCK-03 tests pass)
- `npx tsc --noEmit` — no TypeScript errors
- All 6 V1 IDs in getAllV2Templates() result
- Every template in registry has non-empty sizeLabel matching format regex
- wrapping-paper-v2 name includes "(Flat Sheet)" disambiguation
- wrapping-paper (photo) name includes "(Gift Box)" disambiguation
</verification>

<success_criteria>
- 6 migrated V1 templates in V2 registry with photo-based productBase
- V1 visual fidelity verified with lighting OFF before lighting enhancement applied
- Every V2 template has sizeLabel in locked format
- Wrapping paper variants disambiguated
- All phase tests green
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-templates-complete/01-02-SUMMARY.md`
</output>
